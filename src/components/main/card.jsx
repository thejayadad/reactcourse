// We've named this component Card. Remember, in React, component names should always start with an uppercase letter.

//Arrow Function: Just like with our Header component, we're using an arrow function to define the Card component.

const Card = () => {
    return (
        //div returning a h2 and p tag
      <div>

        <h2>Card Title</h2>
        <p>This is some card content. You can use this area to describe the content of the card.</p>
      </div>
    );
  };
  
  export default Card;
  